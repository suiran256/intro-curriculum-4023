'use strict';

function objsToMapMap(objs,rowProperty,colProperty){
  const mapMap=new Map()
  let prevRow=null
  const objs.forEach(obj=>{
    const row=obj.get(rowProperty)
    if(row!== prevRow) 
  })

}

